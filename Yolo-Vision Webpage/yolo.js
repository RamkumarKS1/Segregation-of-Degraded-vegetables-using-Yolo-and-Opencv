// script.js
document.addEventListener("DOMContentLoaded", () => {
    const button = document.querySelector("button");
    button.addEventListener("click", () => {
      alert("You clicked the Framer-style button!");
    });
  });
  
  function process(vegetable) {
    alert("Processing " + vegetable + "potato");
    // You could redirect, call an API, or do something more advanced here.
  }
  function process(vegetable) {
    alert("Processing " + vegetable + "tomato");
    // You could redirect, call an API, or do something more advanced here.
  }
  function process(vegetable) {
    alert("Processing " + vegetable + "bellpepper");
    // You could redirect, call an API, or do something more advanced here.
  }
  function process(vegetable) {
    alert("Processing " + vegetable + "cucumber");
    // You could redirect, call an API, or do something more advanced here.
  }
  function process(vegetable) {
    alert("Processing " + vegetable + "carrot");}
    // You could redirect, call an API, or do something more advanced here.
// Optional: Add behavior for the "Home" button
document.querySelector('.home-btn').addEventListener('click', () => {
    window.location.href = "index.html"; 
  });
  function goToNextPage() {
    window.location.href = "potato_result.html"; // Replace with your actual page
  }
  function goToNextPage() {
    window.location.href = "tomato_result.html"; // Replace with your actual page
  }
  function goToNextPage() {
    window.location.href = "cucumber_result.html"; // Replace with your actual page
  }
  function goToNextPage() {
    window.location.href = "bellpeper_result.html"; // Replace with your actual page
  }
  function goToNextPage() {
    window.location.href = "carrot_result.html"; // Replace with your actual page
  }
        